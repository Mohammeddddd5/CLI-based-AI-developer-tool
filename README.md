# CLI-based-AI-developer-tool
A Command-line AI tool that generates unit tests for a given function

Instructions:
1. Copy the function you want to generate tests for
2. Paste it into the CLI
3. Press Enter, and it will generate multiple test functions, run them in your file and they will test your function with multiple test cases (including edge cases)
